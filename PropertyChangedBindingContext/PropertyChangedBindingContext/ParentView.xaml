<?xml version="1.0" encoding="UTF-8"?>
<ContentView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PropertyChangedBindingContext"
    x:Class="PropertyChangedBindingContext.ParentView">
    <StackLayout>
        <Button Text="TitleCommand" Command="{Binding TitleCommand}" />
        <Label BackgroundColor="Yellow" Text="{Binding Title}" />
        <Editor x:Name="_editor" BackgroundColor="LightYellow"/>
        <local:ChildView
            BindingContext="{Binding TitleModel}"
            Title="{Binding Title}"/>
    </StackLayout>
</ContentView>
